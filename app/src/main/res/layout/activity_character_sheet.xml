<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/dialog_background"
    android:fitsSystemWindows="true">

    <!-- Основной контент -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Тулбар -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/bir"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:title="Персонаж"
            app:titleTextColor="@color/bir"
            app:navigationIcon="@drawable/ic_menu"/>

        <!-- Вкладки -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bir"
            app:tabTextColor="@color/darkbir"
            app:tabSelectedTextColor="@color/darkbir"
            app:tabIndicatorColor="@color/darkbir"/>

        <!-- Контейнер для фрагментов -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>
    </LinearLayout>

    <!-- Навигационное меню -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/bir"
        app:menu="@menu/navigation_menu"
        app:itemTextColor="@color/darkbir"
        app:itemIconTint="@color/bir"/>

</androidx.drawerlayout.widget.DrawerLayout>